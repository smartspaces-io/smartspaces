This folder is for shell environment variables which modify aspects of the Smart
SmartSpaces Extension Files (.ext) would be placed in this directory.

The localenvironment.rc file can be used to declare environment variables for things like the classpath that
Smart Spaces starts with.

container.args contains special command line flags for the Java Virtual Machine as Smart Spaces starts up.
